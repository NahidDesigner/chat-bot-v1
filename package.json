{
  "name": "ai-chatbot-platform",
  "version": "1.0.0",
  "description": "Multilingual AI Chatbot SaaS Platform",
  "private": true,
  "main": "backend/dist/main.js",
  "scripts": {
    "start": "node backend/dist/main.js",
    "build": "cd backend && npm install && npm run build",
    "dev": "cd backend && npm run start:dev",
    "migrate": "cd backend && npm run migration:run"
  },
  "engines": {
    "node": ">=18.0.0 <23.0.0",
    "npm": ">=9.0.0"
  },
  "dependencies": {},
  "devDependencies": {}
}
